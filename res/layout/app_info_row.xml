<?xml version="1.0" encoding="utf-8"?>
<!-- One Material 3 list item inside the app info bottom sheet: leading icon, headline and
     supporting text. Two-line item, so 72dp tall. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="@dimen/app_info_row_min_height"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="@dimen/app_info_sheet_horizontal_padding"
    android:paddingEnd="@dimen/app_info_sheet_horizontal_padding"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:background="?android:attr/selectableItemBackground">

    <ImageView
        android:id="@android:id/icon"
        android:layout_width="@dimen/app_info_row_icon_size"
        android:layout_height="@dimen/app_info_row_icon_size"
        android:layout_marginEnd="@dimen/app_info_sheet_horizontal_padding"
        android:tint="@color/materialColorOnSurfaceVariant"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Body large -->
        <TextView
            android:id="@android:id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="16sp"
            android:textColor="@color/materialColorOnSurface" />

        <!-- Body medium -->
        <TextView
            android:id="@android:id/summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="14sp"
            android:textColor="@color/materialColorOnSurfaceVariant" />

    </LinearLayout>

</LinearLayout>
